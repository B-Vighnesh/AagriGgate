spring:
  datasource:
    url: ${DB_URL:jdbc:mysql://localhost:3306/app}
    username: ${DB_USERNAME:root}
    password: ${DB_PASSWORD:1234}
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    database-platform: org.hibernate.dialect.MySQLDialect
    properties:
      hibernate:
        format_sql: true
    hibernate:
      ddl-auto: update

  security:
    user:
      name: ${APP_SECURITY_USER:vighnesh}
      password: ${APP_SECURITY_PASSWORD:v123}

  mail:
    host: ${MAIL_HOST:smtp.gmail.com}
    port: ${MAIL_PORT:587}
    username: ${EMAIL_USERNAME:webappfarmer@gmail.com}
    password: ${EMAIL_PASSWORD:syoz atph yahs vfts}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

jwt:
  # your project currently generates dynamic secret,
  # but this is proper fixed fallback
  secret: ${JWT_SECRET:YWFncmlnZ2F0ZS1zZWN1cmUtand0LXNlY3JldC1rZXktMjAyNi1mZWItMjM=}

weather:
  api:
    url: ${WEATHER_API_URL:https://api.open-meteo.com/v1/forecast}

market:
  api:
    url: ${MARKET_API_URL:https://api.data.gov.in/resource/35985678-0d79-46b4-9ed6-6f13308a1d24}
    key: ${MARKET_API_KEY:579b464db66ec23bdd000001602edb9ef1a64cd961329419d730f705}   # your project currently does not use API key

admin:
  username: ${ADMIN_USERNAME:admin}
  password: ${ADMIN_PASSWORD:admin123}